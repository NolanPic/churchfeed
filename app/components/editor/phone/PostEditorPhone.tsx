import Editor from "../Editor";
import styles from "./PostEditorPhone.module.css";

export default function PostEditorPhone() {
  return <Editor placeholder="What's happening?" className={styles.editor} />;
}
